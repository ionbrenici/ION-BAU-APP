function planCell(uidv,d){const abs=db.absences.find(a=>a.userId===uidv&&['Genehmigt','Gemeldet'].includes(a.status)&&inRange(d,a.start,a.end));let h=abs?`<div class="allocation ${abs.type==='vacation'?'absence-vac':abs.type==='sick'?'absence-sick':'absence-free'}">${abs.type==='vacation'?'Urlaub':abs.type==='sick'?'Krank':'Frei'}</div>`:'';h+=db.allocations.filter(a=>a.userId===uidv&&a.date===d).map(a=>`<div class="allocation"><b>${esc(project(a.projectId)?.name||'')}</b><br>${fmtNum(a.hours)} h</div>`).join('');return h||'<span class="tiny muted">–</span>'}
