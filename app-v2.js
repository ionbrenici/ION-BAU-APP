/* ION BAU V2 UI behavior overrides. Keeps V1 data model and workflows intact. */
function companyBrand(){const name=db.company.name||'ION BAU';const initials=name==='ION BAU'?'IB':name.split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase();return `<div class="brand-card"><div class="brand-logo">${db.company.logo?`<img src="${db.company.logo}" alt="${esc(name)} Logo">`:`<span class="initials">${esc(initials)}</span>`}</div><div><div class="brand-title">${esc(name)}</div><div class="product-mark">Baustelle · Büro · Ein System</div></div></div>`}
function projectOverview(p){const maps=p.address?`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(p.address)}`:'';return `<div class="detail-grid"><div class="detail-card"><div class="label">Kunde / Firma</div><div class="value">${esc(p.customer||'–')}</div>${contactLinks(p.customerPhone,p.customerEmail)}</div><div class="detail-card"><div class="label">Ansprechpartner</div><div class="value">${esc(p.contact||'–')}</div>${contactLinks(p.contactPhone,p.contactEmail)}</div><div class="detail-card span2"><div class="label">Baustellenadresse</div><div class="value">${p.address?`<a class="address-link" href="${maps}" target="_blank" rel="noopener">${esc(p.address)}</a>`:'–'}</div>${p.address?`<div class="contact-actions"><a class="btn sm primary" href="${maps}" target="_blank" rel="noopener">${ico('map')} Navigation starten</a></div>`:''}</div></div>`}
function quickActionModal(){const actions=[['Projekt anlegen',"projectForm()",'projects'],['Aufgabe anlegen',"taskForm()",'tasks'],['Material anfordern',"materialForm()",'material'],['Zeiten erfassen',"timeForm()",'time'],['Projektchat öffnen',"go('chats')",'chat']];openModal('Schnellaktion',`<div class="v2-quick-grid">${actions.map(([label,action,icon])=>`<button onclick="closeModal();${action}">${ico(icon)}<span>${label}</span></button>`).join('')}</div>`)}
function moreMenu(){const office=isOffice();const links=office?[['planning','Baustellenplanung','planning'],['staffplan','Mitarbeiterplanung','users'],['tasks','Aufgaben','tasks'],['times','Zeiterfassung','time'],['employees','Mitarbeiter','users'],['materials','Material','material']]:[['tasks','Aufgaben','tasks'],['times','Zeiterfassung','time'],['materials','Material','material'],['me','Mein Bereich','user']];if(isAdmin())links.push(['settings','Einstellungen','settings']);openModal('Mehr',`<div class="v2-quick-grid v2-more-grid">${links.map(([p,l,i])=>`<button onclick="closeModal();go('${p}')">${ico(i)}<span>${l}</span></button>`).join('')}</div>`)}
function mobileNav(){return `<div class="bottom-mobile"><button class="${view.page==='dashboard'?'active':''}" onclick="go('dashboard')">${ico('dashboard')}<span>Heute</span></button><button class="${view.page==='projects'||view.page==='project'?'active':''}" onclick="go('projects')">${ico('projects')}<span>Projekte</span></button><button class="mobile-create" onclick="quickActionModal()" aria-label="Neu">${ico('plus')}<span>Neu</span></button><button class="${view.page==='chats'?'active':''}" onclick="go('chats')">${ico('chat')}<span>Chat</span></button><button onclick="moreMenu()">${ico('menu')}<span>Mehr</span></button></div>`}
function projectsPage(){const ps=activeProjects().slice().sort((a,b)=>a.name.localeCompare(b.name,'de'));return `<div class="v2-projects-page">${pageHead('Projekte',`${ps.length} aktive Projekte`,isOffice()?`<button class="btn primary project-add" onclick="projectForm()">${ico('plus')} <span>Projekt anlegen</span></button>`:'')}<div class="project-list-search">${ico('search')}<input id="projectListSearch" placeholder="Projekt, Kunde oder Ort suchen" oninput="filterProjectCards(this.value)"></div><div class="project-list" id="projectList">${ps.map(p=>`<article class="project-list-card clickrow" data-search="${esc((p.name+' '+p.customer+' '+p.nr+' '+p.address).toLowerCase())}" role="button" tabindex="0" onclick="openProject('${p.id}')"><div class="project-list-main"><div class="project-list-top"><div class="project-list-title">${esc(p.name)}</div>${statusPill(p.status)}</div><div class="project-list-customer">${esc(p.customer||'Kein Kunde hinterlegt')}</div><div class="project-list-meta"><span>${esc(p.nr)}</span>${p.address?`<span class="project-list-address">${ico('map')} ${esc(p.address)}</span>`:''}</div></div><div class="project-list-side"><span class="project-chevron">›</span>${isOffice()?`<button class="project-edit-link" onclick="event.stopPropagation();projectForm('${p.id}')" aria-label="Projekt bearbeiten">${ico('edit')}</button>`:''}</div></article>`).join('')}</div><div id="projectListEmpty" class="card pad muted hidden">Keine passenden Projekte gefunden.</div>${isOffice()?archivedProjectsBlock():''}</div>`}
