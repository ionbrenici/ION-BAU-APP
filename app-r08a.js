function settingsPage(){if(!isAdmin())return'<div class="card pad">Kein Zugriff.</div>';return `${pageHead('Einstellungen','Unternehmen, Branding und Datensicherung')}<div class="grid two-col"><div class="card pad"><h2 class="section-title">Unternehmen</h2><div class="form-grid"><div class="field span2"><label>Firmenname</label><input id="coName" value="${esc(db.company.name)}" placeholder="z. B. MP Concept GmbH"></div><div class="field span2"><label>Logo hochladen</label><input id="coLogo" type="file" accept="image/*" onchange="previewCompanyLogo(this.files[0])"><div id="logoPreview" class="mt8">${db.company.logo?`<img src="${db.company.logo}" style="max-width:190px;max-height:100px;object-fit:contain;border:1px solid var(--line);border-radius:12px;padding:8px">`:'<span class="muted">Noch kein Logo hinterlegt.</span>'}</div></div><div class="field span2"><label>Anschrift</label><input id="coAddress" value="${esc(db.company.address)}"></div><div class="field"><label>Telefon</label><input id="coPhone" value="${esc(db.company.phone)}"></div><div class="field"><label>E-Mail</label><input id="coEmail" value="${esc(db.company.email)}"></div></div><button class="btn primary mt16" onclick="saveCompany()">Unternehmen speichern</button></div><div class="card pad"><h2 class="section-title">Datensicherung</h2><p class="muted">Für die lokale Testversion kannst du alle Daten als JSON sichern und wieder importieren.</p><div class="actions" style="justify-content:flex-start"><button class="btn" onclick="exportBackup()">Backup exportieren</button><label class="btn">Backup importieren<input type="file" accept="application/json" hidden onchange="importBackup(this.files[0])"></label><button class="btn danger" onclick="resetDemo()">Testdaten zurücksetzen</button></div><hr style="border:0;border-top:1px solid var(--line);margin:20px 0"><div><b>Version</b><div class="muted">ION BAU ${VERSION}</div></div></div></div>`}
