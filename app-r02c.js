async function saveAllocation(){const ids=[...document.querySelectorAll('.allocUser:checked')].map(x=>x.value);if(!ids.length)return toast('Mitarbeiter auswählen','err');ids.forEach(id=>db.allocations.push({id:uid('a'),userId:id,projectId:v('allocProject'),date:v('allocDate'),hours:Number(v('allocHours')||8),note:v('allocNote')}));await save();closeModal();render();toast('Einsatz eingeplant')}
function myPlanning(){const w=datesOfWeek(view.weekAnchor);return `${pageHead('Meine Planung',`${fmtDate(w[0])} – ${fmtDate(w[6])}`,`<button class="btn" onclick="shiftWeek(-7)">←</button><button class="btn" onclick="shiftWeek(7)">→</button>`)}<div class="grid">${w.map(d=>`<div class="card pad"><div class="between"><b>${new Date(d+'T12:00:00').toLocaleDateString('de-DE',{weekday:'long',day:'2-digit',month:'2-digit'})}</b></div><div class="mt8">${planCell(user().id,d)}</div></div>`).join('')}</div>`}
