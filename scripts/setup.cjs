// Run by the owner with DATABASE_URL, ADMIN_EMAIL and ADMIN_PASSWORD in a secure environment.
const fs=require('node:fs');const {transaction}=require('../server/storage.cjs'),{hash}=require('../server/auth.cjs');
(async()=>{if(!/^\S+@\S+\.\S+$/.test(process.env.ADMIN_EMAIL||''))throw Error('ADMIN_EMAIL fehlt.');const passwordHash=hash(process.env.ADMIN_PASSWORD);await transaction(async c=>{await c.query(fs.readFileSync('server/schema.sql','utf8'));const data={company:{name:'MP Concept GmbH',logo:'',address:'',phone:'',email:'',creditHours:8,extraHolidays:[]},users:[{id:'u_admin',first:'Ion',last:'Brenici',email:process.env.ADMIN_EMAIL,phone:'',role:'admin',active:true,passwordHash,entry:'',vacationAnnual:30,notes:''}],projects:[],tasks:[],chat:[],timeEntries:[],monthlyTargets:{},monthClosures:[],absences:[],allocations:[],phases:[],materials:[],notifications:[],audit:[]};await c.query('INSERT INTO ion_state(id,data) VALUES (1,$1) ON CONFLICT (id) DO NOTHING',[data]);});console.log('Schema und initialer Admin eingerichtet; bestehende Daten wurden nicht überschrieben.');process.exit(0);})().catch(e=>{console.error(e.message);process.exit(1)});
