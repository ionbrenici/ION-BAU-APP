function timeTable(arr){return `<div class="table-wrap"><table class="table"><thead><tr><th>Datum</th><th>Mitarbeiter</th><th>Projekt</th><th>Stunden</th><th>Tätigkeit</th><th>Notiz</th></tr></thead><tbody>${arr.length?arr.sort((a,b)=>b.date.localeCompare(a.date)).map(t=>`<tr><td>${fmtDate(t.date)}</td><td>${esc(userName(t.userId))}</td><td>${esc(project(t.projectId)?.name||'–')}</td><td><b>${fmtNum(t.hours)} h</b></td><td>${esc(t.activity||'–')}</td><td>${esc(t.note||'')}</td></tr>`).join(''):'<tr><td colspan="6" class="muted">Keine Einträge.</td></tr>'}</tbody></table></div>`}
function exportTimesCSV(){const rows=[['Datum','Mitarbeiter','Projekt','Stunden','Tätigkeit','Notiz'],...db.timeEntries.map(t=>[t.date,userName(t.userId),project(t.projectId)?.name||'',t.hours,t.activity||'',t.note||''])];const csv=rows.map(r=>r.map(x=>'"'+String(x).replace(/"/g,'""')+'"').join(';')).join('\n');downloadBlob('ION_BAU_Zeiten.csv','text/csv;charset=utf-8','\ufeff'+csv)}
function planningPage(){if(!isOffice())return myPlanning();const week=datesOfWeek(view.weekAnchor);return `${pageHead('Mitarbeiterplanung',`${fmtDate(week[0])} – ${fmtDate(week[6])}`,`<button class="btn" onclick="shiftWeek(-7)">← Vorherige</button><button class="btn" onclick="view.weekAnchor=today();render()">Heute</button><button class="btn" onclick="shiftWeek(7)">Nächste →</button><button class="btn primary" onclick="allocationForm()">${ico('plus')} Einsatz</button>`)}<div class="table-wrap" style="border:0"><div class="plan-grid"><div class="plan-head">Mitarbeiter</div>${week.map(d=>`<div class="plan-head">${new Date(d+'T12:00:00').toLocaleDateString('de-DE',{weekday:'short',day:'2-digit',month:'2-digit'})}</div>`).join('')}${db.users.filter(u=>u.active&&u.role==='employee').map(u=>`<div class="plan-cell plan-name">${esc(u.first+' '+u.last)}</div>${week.map(d=>`<div class="plan-cell">${planCell(u.id,d)}</div>`).join('')}`).join('')}</div></div><div class="card pad mt16"><h2 class="section-title">Abwesenheiten</h2><div class="actions" style="justify-content:flex-start"><button class="btn sm" onclick="absenceForm('vacation')">Urlaub</button><button class="btn sm" onclick="absenceForm('sick')">Krank</button><button class="btn sm" onclick="absenceForm('free')">Frei</button></div>${absenceAdminTable()}</div>`}
function shiftWeek(days){const d=new Date(view.weekAnchor+'T12:00:00');d.setDate(d.getDate()+days);view.weekAnchor=d.toISOString().slice(0,10);render()}
