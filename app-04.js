function dashboard(){const ps=activeProjects(),tasks=db.tasks.filter(t=>t.status!=='Erledigt'&&(isOffice()||t.assignees.includes(user().id))),mats=db.materials.filter(m=>!['Geliefert','Storniert'].includes(m.status)&&(isOffice()||project(m.projectId)?.members.includes(user().id))),week=datesOfWeek();const siteToday=ps.map(p=>{const phase=db.phases.find(ph=>ph.projectId===p.id&&inRange(today(),ph.start,ph.end));const alloc=db.allocations.filter(a=>a.projectId===p.id&&a.date===today()&&(isOffice()||a.userId===user().id));return {p,phase,alloc}}).filter(x=>isOffice()?(x.phase||x.alloc.length):x.alloc.length);return `<div class="mobile-today">${mobileToday(siteToday,tasks,mats)}</div><div class="desktop-dashboard">${pageHead(isOffice()?'Dashboard':'Heute',isOffice()?'Was läuft, wo fehlt etwas und was ist zu erledigen?':'Dein heutiger Überblick')}<div class="grid kpis"><div class="card kpi clickrow" onclick="go('projects')"><div class="label">Aktive Projekte</div><div class="num">${ps.length}</div></div><div class="card kpi clickrow" onclick="go('tasks')"><div class="label">Offene Aufgaben</div><div class="num">${tasks.length}</div></div><div class="card kpi clickrow" onclick="go('tasks')"><div class="label">Überfällig</div><div class="num">${overdueTasks().length}</div></div><div class="card kpi clickrow" onclick="go('materials')"><div class="label">Offenes Material</div><div class="num">${mats.length}</div></div><div class="card kpi clickrow" onclick="go('planning')"><div class="label">Mitarbeiter heute</div><div class="num">${new Set(db.allocations.filter(a=>a.date===today()).map(a=>a.userId)).size}</div></div></div><div class="grid two-col"><div class="card pad"><div class="between"><h2 class="section-title">Heute auf den Baustellen</h2>${isOffice()?`<button class="btn sm" onclick="go('planning')">Planung öffnen</button>`:''}</div><div class="list">${siteToday.length?siteToday.map(x=>`<div class="list-row clickrow" onclick="openProject('${x.p.id}')"><div><div class="list-title">${esc(x.p.name)}</div><div class="small muted">${x.phase?esc(x.phase.name):'Einsatz'} · ${x.alloc.length} Mitarbeiter${x.phase?` · Bedarf ${x.phase.required}`:''}</div>${x.phase&&x.alloc.length<x.phase.required?`<div class="warning">⚠ Unterbesetzt ${x.alloc.length}/${x.phase.required}</div>`:''}</div><span>›</span></div>`).join(''):`<div class="muted">Für heute sind keine Baustelleneinsätze geplant.</div>`}</div></div><div class="card pad"><h2 class="section-title">Handlungsbedarf</h2><div class="list">${overdueTasks().slice(0,5).map(t=>`<div class="list-row clickrow" onclick="taskDetail('${t.id}')"><div><div class="list-title">${esc(t.title)}</div><div class="small muted">${esc(project(t.projectId)?.name||'')} · fällig ${fmtDate(t.due)}</div></div>${statusPill('Überfällig')}</div>`).join('')||'<div class="muted">Aktuell nichts Kritisches.</div>'}${isOffice()?missingTimeWarnings():''}</div></div></div><div class="grid two-col mt16"><div class="card pad"><div class="between"><h2 class="section-title">Meine Aufgaben</h2><button class="btn sm" onclick="go('tasks')">Alle Aufgaben</button></div>${taskMiniList(db.tasks.filter(t=>t.status!=='Erledigt'&&t.assignees.includes(user().id)).slice(0,6))}</div><div class="card pad"><h2 class="section-title">Diese Woche</h2><div class="small muted">${fmtDate(week[0])} – ${fmtDate(week[6])}</div><div class="mt12">${week.map(d=>`<div class="between" style="padding:7px 0;border-top:1px solid #edf1f6"><span>${new Date(d+'T12:00:00').toLocaleDateString('de-DE',{weekday:'short',day:'2-digit',month:'2-digit'})}</span><span class="small muted">${db.allocations.filter(a=>a.date===d&&(isOffice()||a.userId===user().id)).length} Einsätze</span></div>`).join('')}</div></div></div></div>`}
function mobileToday(siteToday,tasks,mats){const u=user(),mine=db.allocations.filter(a=>a.date===today()&&(isOffice()||a.userId===u.id)),urgent=tasks.filter(t=>t.due&&t.due<=today()).sort((a,b)=>(a.due||'').localeCompare(b.due||'')).slice(0,4),openMats=mats.slice(0,3);return `<div class="today-head"><div><div class="today-date">${new Date(today()+'T12:00:00').toLocaleDateString('de-DE',{weekday:'long',day:'2-digit',month:'long'})}</div><h1>Hallo ${esc(u.first)}</h1></div><button class="today-bell" onclick="notificationModal()">${ico('bell')}${unreadNotifications()?`<b>${unreadNotifications()}</b>`:''}</button></div><section class="today-section"><div class="today-section-head"><h2>${isOffice()?'Heute auf Baustellen':'Dein Einsatz heute'}</h2>${isOffice()?`<button onclick="go('planning')">Planung</button>`:''}</div>${mine.length?mine.map(a=>{const p=project(a.projectId);return p?`<article class="today-site" onclick="openProject('${p.id}')"><div class="today-site-top"><div><strong>${esc(p.name)}</strong><span>${esc(p.nr)}</span></div><span class="today-arrow">›</span></div><div class="today-site-address">${esc(p.address||'Keine Adresse hinterlegt')}</div><div class="today-site-actions">${p.address?`<a href="https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(p.address)}" target="_blank" onclick="event.stopPropagation()">${ico('map')} Navigation</a>`:''}<button onclick="event.stopPropagation();openProject('${p.id}','chat')">Chat</button><button onclick="event.stopPropagation();openProject('${p.id}','tasks')">Aufgaben</button></div></article>`:''}).join(''):`<div class="today-empty">${isOffice()?'Heute sind noch keine Mitarbeiter eingeplant.':'Für heute ist kein Einsatz eingetragen.'}</div>`}</section><section class="today-section"><div class="today-section-head"><h2>${isOffice()?'Dringend':'Deine Aufgaben'}</h2><button onclick="go('tasks')">Alle</button></div>${urgent.length?urgent.map(t=>`<div class="today-row" onclick="taskDetail('${t.id}')"><div><strong>${esc(t.title)}</strong><span>${esc(project(t.projectId)?.name||'')} · ${t.due===today()?'heute':fmtDate(t.due)}</span></div>${statusPill(t.priority==='Hoch'?'Hoch':t.status)}<i>›</i></div>`).join(''):'<div class="today-empty">Keine dringenden Aufgaben.</div>'}</section>${openMats.length?`<section class="today-section"><div class="today-section-head"><h2>Material</h2><button onclick="go('materials')">Alle</button></div>${openMats.map(m=>`<div class="today-row" onclick="go('materials')"><div><strong>${esc(project(m.projectId)?.name||'Material')}</strong><span>${esc(m.status)}${m.needed?` · benötigt ${fmtDate(m.needed)}`:''}</span></div><i>›</i></div>`).join('')}</section>`:''}</div>`}
function missingTimeWarnings(){const y=new Date();y.setDate(y.getDate()-1);const d=y.toISOString().slice(0,10);const scheduled=new Set(db.allocations.filter(a=>a.date===d).map(a=>a.userId));const entered=new Set(db.timeEntries.filter(t=>t.date===d).map(t=>t.userId));const miss=[...scheduled].filter(x=>!entered.has(x));return miss.map(id=>`<div class="list-row"><div><div class="list-title">Fehlende Stunden</div><div class="small muted">${esc(userName(id))} · ${fmtDate(d)}</div></div>${statusPill('Prüfen')}</div>`).join('')}
function taskMiniList(arr){return `<div class="list">${arr.length?arr.map(t=>`<div class="list-row clickrow" role="button" tabindex="0" onclick="taskDetail('${t.id}')" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();taskDetail('${t.id}')}" title="Aufgabe öffnen"><div><div class="list-title">${esc(t.title)}</div><div class="small muted">${esc(project(t.projectId)?.name||'')} · ${fmtDate(t.due)}</div></div>${statusPill(t.status)}</div>`).join(''):'<div class="muted">Keine Aufgaben.</div>'}</div>`}
function projectsPage(){const ps=activeProjects().slice().sort((a,b)=>a.name.localeCompare(b.name,'de'));return `${pageHead('Projekte',`${ps.length} aktive Projekte`,isOffice()?`<button class="btn primary project-add" onclick="projectForm()">${ico('plus')} <span>Projekt anlegen</span></button>`:'')}<div class="project-list-search">${ico('search')}<input id="projectListSearch" placeholder="Projekte oder Kunden suchen" oninput="filterProjectCards(this.value)"></div><div class="project-list" id="projectList">${ps.map(p=>`<article class="project-list-card clickrow" data-search="${esc((p.name+' '+p.customer+' '+p.nr+' '+p.address).toLowerCase())}" role="button" tabindex="0" onclick="openProject('${p.id}')" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();openProject('${p.id}')}" title="Projekt öffnen"><div class="project-list-main"><div class="project-list-title">${esc(p.name)}</div><div class="project-list-customer">${esc(p.customer||'Kein Kunde hinterlegt')}</div><div class="project-list-sub"><span>${esc(p.nr)}</span><span>·</span><span>${statusPill(p.status)}</span>${p.address?`<span class="project-list-address">· ${esc(p.address)}</span>`:''}</div></div><div class="project-list-side"><span class="project-chevron">›</span>${isOffice()?`<button class="project-edit-link" onclick="event.stopPropagation();projectForm('${p.id}')" aria-label="Projekt bearbeiten">${ico('edit')}</button>`:''}</div></article>`).join('')}</div><div id="projectListEmpty" class="card pad muted hidden">Keine passenden Projekte gefunden.</div>${isOffice()?archivedProjectsBlock():''}`}
function filterProjectCards(q){const s=(q||'').trim().toLowerCase();const cards=[...document.querySelectorAll('.project-list-card')];let visible=0;cards.forEach(c=>{const show=!s||c.dataset.search.includes(s);c.style.display=show?'':'none';if(show)visible++});document.getElementById('projectListEmpty')?.classList.toggle('hidden',visible>0)}
function archivedProjectsBlock(){const a=db.projects.filter(p=>p.archived);if(!a.length)return'';return `<div class="card pad mt16"><h2 class="section-title">Archiv</h2>${a.map(p=>`<div class="list-row"><div><b>${esc(p.name)}</b><div class="small muted">${esc(p.nr)}</div></div><button class="btn sm" onclick="toggleArchive('${p.id}')">Reaktivieren</button></div>`).join('')}</div>`}
function openProject(id,tab='overview'){view.page='project';view.projectId=id;view.projectTab=tab;view.selectedFolder=null;render()}
