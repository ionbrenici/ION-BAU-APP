function allocationForm(){const us=db.users.filter(u=>u.active&&u.role==='employee'),ps=activeProjects();openModal('Mitarbeiter einplanen',`<div class="form-grid"><div class="field span2"><label>Mitarbeiter (mehrere möglich)</label><div class="card pad" style="box-shadow:none">${us.map(u=>`<label style="display:inline-flex;gap:7px;margin:4px 14px 4px 0"><input class="allocUser" type="checkbox" value="${u.id}"> ${esc(u.first+' '+u.last)}</label>`).join('')}</div></div><div class="field"><label>Projekt</label><select id="allocProject">${ps.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('')}</select></div><div class="field"><label>Datum</label><input id="allocDate" type="date" value="${today()}"></div><div class="field"><label>Stunden / Anteil</label><input id="allocHours" type="number" step="0.25" value="8"></div><div class="field"><label>Notiz</label><input id="allocNote"></div></div>`,`<button class="btn" onclick="closeModal()">Abbrechen</button><button class="btn primary" onclick="saveAllocation()">Einplanen</button>`)}
